# HAVENLY 라이브 auth 포인터 재복구 및 버튼 백엔드 검증 — 2026-04-16 15:42 UTC

## 필수 결정 단계
1. 현재 핵심 blocker 한 줄: live runtime auth 포인터가 죽은 public tunnel(`https://dbf8240d1bc134.lhr.life`)을 가리켜 실제 로그인/회원가입 버튼이 백엔드에 닿지 못했다.
2. 가장 작은 다음 행동 후보:
   - 로컬 sqlite auth 서버 헬스를 다시 확인한다.
   - 새 public tunnel 을 열고 공개 backend 계약을 확인한다.
   - runtime auth 포인터를 새 tunnel 로 바꾸고 좁게 실클릭 검증한다.
3. 선택한 단일 행동: 새 public tunnel 을 열고 runtime auth 포인터를 갱신한 뒤 로그인/회원가입 실클릭이 실제 backend POST 로 이어지는지 다시 확인한다.

## 직접 확인한 원인
- 기존 runtime 파일:
  - `public/havenly-auth-config.js`
  - `docs/havenly-auth-config.js`
- 기존 값:
  - `https://dbf8240d1bc134.lhr.life`
- 헬스 체크 결과:
  - `http://127.0.0.1:4175/api/auth/health` → `200`
  - `https://dbf8240d1bc134.lhr.life/api/auth/health` → `503`

즉 프런트 submit 코드 자체보다 **live backend pointer 만료**가 직접 원인이었다.

## 복구 조치
### 1) 새 public backend tunnel 개설
- localhost.run managed session: `clear-tidepool`
- 새 public auth base URL:
  - `https://721e6bfef031b8.lhr.life`

### 2) 공개 backend 계약 확인
- `GET /api/auth/health` → `200`
- `OPTIONS /api/auth/signup` with `Origin: https://neujeans.github.io` → `204`
- `POST /api/auth/signup` with `Origin: https://neujeans.github.io` → `200`
- 응답에 `SameSite=None; Secure` 쿠키와 CORS 허용 헤더 확인

### 3) runtime auth 포인터 갱신
아래 두 파일을 새 tunnel 로 교체:
- `public/havenly-auth-config.js`
- `docs/havenly-auth-config.js`

새 값:
```js
window.__HAVENLY_AUTH_CONFIG__ = {
  apiBaseUrl: 'https://721e6bfef031b8.lhr.life',
  credentialsMode: 'include',
}
```

### 4) Pages 산출물 재생성
실행:
```bash
npm run build:pages
```
결과: 성공 (`docs/` 재생성)

## 좁은 검증
### 1) auth 관련 테스트
실행:
```bash
node --test src/components/auth-config.test.js src/components/auth-submit.test.js server/auth-http-server.test.js server/auth-persistent-store.test.js
```
결과:
- `60/60` 통과

### 2) 실제 버튼 실클릭 검증 (Playwright, focused)
배경:
- loopback preview 는 runtime external auth URL 을 무시할 수 있으므로,
- preview URL 에 `?authApiBaseUrl=...&authCredentials=include` 를 명시해 실제 backend path 를 강제로 사용했다.

검증 URL:
- `http://127.0.0.1:4179/github-pages-react-hello/?authApiBaseUrl=https%3A%2F%2F721e6bfef031b8.lhr.life&authCredentials=include#/layout`

#### 회원가입 결과
- 요청:
  - `GET https://721e6bfef031b8.lhr.life/api/auth/session` → `401`
  - `POST https://721e6bfef031b8.lhr.life/api/auth/signup` → `200`
- UI 결과:
  - 제목: `실클릭 테스트 계정 연결됨`
  - 본문: `게스트 초안을 계정에 연결했어요. 복원됨: 배치 5개 · 추천 초안. 현재 단계: 인증 준비 완료. 레이아웃 작업 이어가기 (프로젝트 레이아웃 보드) 단계까지 이어서 진행할 수 있어요.`
- 스크린샷:
  - `notes/auth-signup-real-click-2026-04-16-15-38-05-280UTC.png`

#### 로그인 결과
- 동일 계정 새 브라우저 컨텍스트에서 재검증
- 요청:
  - `GET https://721e6bfef031b8.lhr.life/api/auth/session` → `401`
  - `POST https://721e6bfef031b8.lhr.life/api/auth/login` → `200`
- UI 결과:
  - 제목: `실클릭 테스트 계정 연결됨`
  - 본문: `게스트 초안을 계정에 연결했어요. 복원됨: 배치 5개 · 추천 초안. 현재 단계: 인증 준비 완료. 레이아웃 작업 이어가기 (프로젝트 레이아웃 보드) 단계까지 이어서 진행할 수 있어요.`
- 스크린샷:
  - `notes/auth-login-real-click-2026-04-16-15-38-05-280UTC.png`

결론:
- `회원가입` 버튼 → 실제 `POST /api/auth/signup` 확인
- `로그인` 버튼 → 실제 `POST /api/auth/login` 확인
- 둘 다 프런트 전용 scaffold 가 아니라 실제 backend 응답으로 UI 상태가 바뀌는 것까지 재확인

## Gemini review
- 스킬 참고: `~/.npm-global/lib/node_modules/openclaw/skills/gemini/SKILL.md`
- default preview model 은 `429 MODEL_CAPACITY_EXHAUSTED` 로 실패
- retry 성공 모델: `gemini-2.5-flash`
- 리뷰 파일:
  - `ai-reviews/gemini-review-auth-runtime-pointer-2026-04-16_1541UTC.md`

핵심 리뷰 포인트:
- runtime auth pointer fragility
- backend API contract mismatch risk
- docs/build delivery risk
- post-push real login/logout/session sanity check 필요

## Secret scan
실행:
```bash
npm run security:secrets
```
결과:
- TruffleHog `verified_secrets: 0`
- TruffleHog `unverified_secrets: 0`

## 현재 상태
- live runtime auth pointer: **복구됨 (로컬 파일 기준)**
- login button backend path: **실클릭 확인 완료**
- signup button backend path: **실클릭 확인 완료**
- Gemini review: **완료**
- secret scan: **완료**

## 남은 직접 행동
1. 의도한 auth/deploy slice 만 stage 한다.
2. commit + push 한다.
3. push 후 GitHub Pages live bare URL 이 새 `721e6bfef031b8.lhr.life` 포인터를 서빙하는지 확인한다.
4. 가능하면 live bare URL 에서 로그인/회원가입 실클릭을 한 번 더 확인한다.
