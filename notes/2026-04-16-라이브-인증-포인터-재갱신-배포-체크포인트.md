# HAVENLY 라이브 인증 포인터 재갱신 + 배포 체크포인트 — 2026-04-16 09:34 UTC

## 필수 결정 단계
1. 최우선 요구 재진술: bare live GitHub Pages URL의 로그인/회원가입 버튼이 다시 실제 공개 auth 백엔드로 진행되게 만든다.
2. 가장 작은 다음 행동 후보:
   - 현재 dead tunnel을 새 public auth tunnel로 교체한다.
   - 새 public auth backend가 GitHub Pages origin에서 실제 signup/session 쿠키 왕복을 받는지 검증한다.
   - 이미 진행 중인 surface trim/app-shell 정리를 build + Pages 산출물까지 반영한다.
3. 선택한 단일 행동: 먼저 새 public auth tunnel을 열고, 공개 backend 계약을 검증한 뒤 runtime 포인터와 Pages 산출물을 갱신한다.

## 이번 런에서 직접 확인한 사실
- 기존 배포 auth 포인터 `https://07dbd559662b11.lhr.life` 는 `503 no tunnel here :(` 상태였다.
- 로컬 sqlite auth backend 는 여전히 건강했다.
  - `http://127.0.0.1:4175/api/auth/health` ✅
- 새 localhost.run 공개 tunnel을 열었다.
  - exec session: `gentle-forest`
  - public auth base URL: `https://90106d02d07e07.lhr.life`
- 새 public backend 계약을 GitHub Pages origin 기준으로 직접 검증했다.
  - `GET /api/auth/health` ✅
  - `OPTIONS /api/auth/signup` with `Origin: https://neujeans.github.io` ✅
  - `POST /api/auth/signup` 실쿠키 발급 (`SameSite=None; Secure`) ✅
  - `GET /api/auth/session` cookie jar 재사용 왕복 ✅

## 이번 런에서 적용한 변경
- runtime auth 포인터 갱신:
  - `public/havenly-auth-config.js`
  - `docs/havenly-auth-config.js`
  - 새 값: `https://90106d02d07e07.lhr.life`
- 기존 진행 중이던 `src/app-shell.jsx` 정리 상태를 유지했다.
  - 불필요한 apartment exploration/search/browsing 관련 연결/상수/overlay 의존성 제거 방향
  - 레이아웃/인증 중심 표면으로 수렴하는 diff 유지

## 검증 실행 결과
- `npm test` ✅ (261/261 통과)
- `npm run build:pages` ✅
- `npm run security:secrets` ✅
  - TruffleHog verified 0 / unverified 0

## 현재 배포 직전 상태
- working tree 에 남아 있는 핵심 변경:
  - `src/app-shell.jsx`
  - `public/havenly-auth-config.js`
  - `docs/havenly-auth-config.js`
- build 산출물은 `docs/` 로 동기화 완료.
- 남은 직접 경로:
  1. 변경 commit
  2. `main` push
  3. GitHub Pages runtime file 전파 확인
  4. bare live URL `https://neujeans.github.io/github-pages-react-hello/#layout` 에서 로그인/회원가입 실동작 재검증

## 주의
- 이 public tunnel 역시 임시 자원이라 배포/검증 전에 만료될 수 있다.
- 만료되면 같은 순서로 새 tunnel을 다시 열고 runtime 포인터만 재갱신하면 된다.
