# HAVENLY 라이브 auth 포인터 재갱신 + 3페이지 상태 체크포인트 — 2026-04-16 09:57 UTC

## 필수 결정 단계
1. 최우선 요구 재진술: bare live GitHub Pages URL의 로그인/회원가입 버튼이 다시 실제 공개 auth 백엔드로 진행되게 만든다.
2. 가장 작은 다음 행동 후보:
   - 죽은 public auth 포인터를 새 public tunnel로 교체한다.
   - 기존 3 page module 분리가 실제 shell에 연결돼 있는지 확인한다.
   - push 전 최소 검증(test/build/secrets)을 통과시킨다.
3. 선택한 단일 행동: 먼저 새 public tunnel을 열고 auth 포인터를 교체한 뒤 최소 검증까지 마친다.

## 이번 런에서 직접 확인한 사실
- 배포 기본 auth 포인터 `https://90106d02d07e07.lhr.life` 는 `503` 이었다.
- 로컬 sqlite auth 서버는 여전히 건강했다.
  - `http://127.0.0.1:4175/api/auth/health` → `200`
- 새 localhost.run 공개 tunnel을 열었다.
  - exec session: `salty-forest`
  - public auth base URL: `https://dca28eddd5713d.lhr.life`
- 새 public auth backend 계약 확인:
  - `GET /api/auth/health` → `200`
  - `OPTIONS /api/auth/signup` with `Origin: https://neujeans.github.io` → `204`
  - `Access-Control-Allow-Origin: https://neujeans.github.io`
  - `Access-Control-Allow-Credentials: true`
- shell/page 구조 상태 확인:
  - `src/app-shell.jsx` 는 이미 `renderScreen()` + page module import 구조를 사용 중
  - 연결 중인 3 page-level module:
    - `src/pages/recommendation-onboarding-pages.jsx`
    - `src/pages/layout-editor-page.jsx`
    - `src/pages/catalog-shopping-pages.jsx`
  - 즉, 현재 `main.jsx` 는 thin entry이고, page bulk는 module 쪽으로 분리된 상태다.
- apartment exploration/search/browsing UI 표면은 retired/final surface 문구와 layout-only surface 중심으로 이미 축소돼 있었다.

## 이번 런에서 적용한 변경
- runtime auth 포인터 갱신:
  - `public/havenly-auth-config.js`
  - `docs/havenly-auth-config.js`
  - 새 값: `https://dca28eddd5713d.lhr.life`
- 기존 local diff 유지:
  - `src/app-shell.jsx` 의 `QuickViewModal` 렌더 제거 diff 유지
  - browsing quick-view 잔재를 더 줄이는 방향의 소규모 삭제

## 검증 실행 결과
- `npm test` ✅ (`261/261` pass)
- `npm run build:pages` ✅
- `npm run security:secrets` ✅
  - TruffleHog verified `0`
  - TruffleHog unverified `0`

## 다음 직접 경로
1. git 상태 확인
2. commit
3. `main` push
4. GitHub Pages runtime file 전파 확인
5. bare live URL `https://neujeans.github.io/github-pages-react-hello/#layout` 에서 실제 auth 동작 재검증

## 주의
- 이번 public tunnel도 임시 자원이다. 배포 전파/검증 중 만료되면 같은 순서로 다시 tunnel만 갱신하면 된다.
