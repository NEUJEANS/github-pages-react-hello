# 2026-04-16 프론트엔드 auth 상태문구 계약정리 체크포인트 (1300 UTC)

## 이번 슬라이스 목표
현재 프론트엔드 auth-entry 흐름은 유지한 채, backend 냄새가 남아 있던 UI 계약을 조금 더 프론트엔드 전용으로 정리한다.

## 이번에 한 일
- `src/components/auth-flow-state.js`
  - `buildAuthStatusCopy` 시그니처에서 실제로 쓰이지 않던 `connectionSummary` 인자를 제거했다.
  - 상태 문구 생성 계약을 plain text 기준으로 정리했다.
- `src/components/auth-modal.jsx`
  - `authStatusMessage.body`처럼 object 형태를 기대하던 잘못된 렌더링을 `authStatusCopy` plain text 렌더링으로 정리했다.
  - `AuthCredentialsView` / `LoginModal` prop 이름을 `authStatusCopy` 기준으로 맞췄다.
  - shell에서 내려오던 dead prop에 맞춰 생긴 꼬임을 정리하면서 JSX tail 손상도 함께 복구했다.
- `src/app-shell.jsx`
  - modal에 실제로 쓰이지 않던 auth connection drift 계산/prop 전달을 제거했다.
  - shell -> modal auth 상태문구 전달을 `authStatusCopy` 기준으로 정리했다.
  - 정리 과정에서 파일 하단 중복 JSX 꼬임을 제거해 빌드 가능 상태로 복구했다.

## 의도적으로 하지 않은 일
- backend auth 서버 복구
- live tunnel / cookie / CORS / proxy 재검증
- remote auth wiring 재도입
- auth persistence 내부 shape 대수술

## 검증
- narrow auth tests
  - `node --test src/components/auth-flow-state.test.js src/components/auth-submit.test.js src/components/auth-verification.test.js src/components/auth-session-view-state.test.js`
  - 결과: pass 39 / fail 0
- frontend build
  - `npm run build`
  - 결과: 성공

## 현재 판단
- auth modal 상태문구는 이제 실제 렌더링 계약과 맞는다.
- shell -> modal 경로에서 쓰이지 않던 backend/drift 성격 props를 줄여 React-level auth 흐름이 조금 더 단순해졌다.
- 다만 auth session/view state 내부에는 여전히 connection/payload-preview 성격의 backend-oriented 흔적이 남아 있다.

## 다음 직접 단계
1. `auth-session-view-state.js`에서 실제 UI가 쓰지 않는 connection/payload preview 계약을 제거하거나 축소한다.
2. 그 다음 `app-shell.jsx`의 auth 관련 계산을 page/module 또는 hook 경계로 더 떼어내서 shell을 줄인다.
3. 이후 layout/auth/save-restore 쪽에서 backend 용어를 계속 frontend scaffold 용어로 치환한다.
