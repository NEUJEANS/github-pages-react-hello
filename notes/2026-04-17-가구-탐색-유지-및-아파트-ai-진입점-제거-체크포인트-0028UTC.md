# 2026-04-17 가구 탐색 유지 및 아파트/AI 진입점 제거 체크포인트 (0028 UTC)

## 이번 슬라이스 목표
- 가구 탐색/발견/선택 UI는 유지 또는 복구
- 레이아웃 보드와 가구 배치 흐름은 그대로 유지
- 아파트 평면도/아파트 탐색 성격의 진입점만 제거 또는 안전한 가구 홈으로 우회

## 실제 수정 내용
1. `src/pages/final-surface-page.jsx`
   - 홈 컬렉션의 `AI 추천으로 이동` 카드를 제거
   - 같은 자리를 가구 중심의 계정/찜 이어보기 액션으로 교체
   - 카탈로그 사이드바의 `AI 추천` 바로가기를 제거
   - 가구 홈/카탈로그/보드 진입은 유지
2. `src/components/spa-hash-navigation-state.js`
   - `#ai`, `#space` 해시를 더 이상 아파트/AI 화면으로 보내지 않고 `home` 으로 해석
   - 화면 메타에서도 `ai` 를 제거
3. `src/app-shell.jsx`
   - 죽은 `AiRecommendPage` 라우트 분기를 제거
   - 해시 동기화 시 stale hash 를 canonical hash 로 정규화하도록 보강
   - 결과적으로 `#ai` 진입도 실제로 `#home` 으로 정리됨
4. `npm run build:pages`
   - 최신 번들을 `docs/` 로 동기화해 GitHub Pages 배포 산출물도 소스 변경과 맞춤

## 검증
### 좁은 테스트
- `node --test src/components/spa-hash-navigation-state.test.js src/components/furniture-browse-state.test.js` ✅
- `npm run build` ✅
- `npm run build:pages` ✅

### 실제 클릭 검증 (Browser tool 장애로 로컬 Playwright 대체)
브라우저 툴이 gateway timeout 으로 사용 불가해서 로컬 preview + Playwright 로 대체 검증함.

검증 경로:
1. 홈 열기
2. `가구 둘러보기` 클릭
3. 카탈로그에서 첫 상품 `보드에 배치`
4. `배치 보드 열기` 클릭
5. 레이아웃 보드 도달 확인
6. 직접 `#ai` 로 들어가도 홈으로 정규화되는지 확인

결과:
- 홈에 `AI 추천으로 이동` 없음 ✅
- 카탈로그에 `AI 추천` 바로가기 없음 ✅
- 보드 도달 (`#layout`) ✅
- 직접 `#ai` 접근 시 `#home` 으로 정규화 ✅

아티팩트:
- `notes/20260417-002448Z-furniture-browse-layout-no-ai-local-validation.json`
- `notes/20260417-002448Z-furniture-browse-layout-no-ai-local-validation.png`

## Gemini 리뷰
- 파일: `ai-reviews/gemini-review-2026-04-17_0026UTC-furniture-no-ai-slice.md`
- 요약: 가구 중심 내비게이션 강화와 레거시 해시 리다이렉트는 긍정적이며, 다음으로는 남은 apartment-specific 상태 정리 여부를 점검하라고 제안함.

## 시크릿 스캔
- `npm run security:secrets` ✅
- TruffleHog 결과: verified/unverified secrets 모두 0

## 주의 / 다음 후보
- `src/pages/auth-entry-page.jsx` 파일 자체는 아직 남아 있지만 현재 라우트에서는 사용하지 않음
- 다음 소형 체크포인트 후보는 apartment-specific 내부 상태/문구를 더 정리할지 판단하는 것
